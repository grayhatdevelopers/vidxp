Add managed video snippets:

- create source-preserving or broadly compatible MP4 snippets by media ID
- reject intervals outside the source or configured duration limit
- reuse ready output for the same media checksum, interval, and profile
