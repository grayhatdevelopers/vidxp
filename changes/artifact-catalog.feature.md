Add managed actor-overlay artifacts:

- publish actor overlay videos under opaque artifact IDs
- bind overlays to their media and index-generation identity
- validate rendered video before marking an artifact ready
- enforce the requested output profile before publication
- resolve source playback and artifact delivery through confined internal handles
- keep storage keys and local paths out of shared command and result schemas
